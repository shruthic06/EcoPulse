{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "node_modules/.bin/tsc -b packages/shared packages/backend",
  "functions": {
    "api/server.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/server" }
  ]
}
